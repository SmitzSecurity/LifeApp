# Mobile forms and dates

September 15, 2026. Browser navigation is 64px high with no added bottom inset. Installed/fullscreen display modes retain the device safe-area inset. The shared viewport shell remains: only the workspace scrolls; header, navigation and the floating response button stay outside its scroll flow. Do not subtract a guessed Firefox toolbar height. Physical Android Firefox is not reproduced by desktop viewport emulation.

Workouts has one + New menu with Manual Build and AI Build; existing drafts resume through it. Budget's Build with AI link sits beside Budget goals at the footer. Closing tools still discards drafts unless a write is in flight or unconfirmed.

NumericInput preserves raw empty and decimal drafts. Unfinished required numbers and invalid optional numbers use a local NaN marker; both API clients reject nonfinite numbers before JSON serialization, which otherwise converts NaN to null. Optional empty numbers remain null. Do not coerce an empty input to zero, clamp while typing, or silently save a previous number. Preserve independent version conflicts and exact unknown-outcome retry payloads. Server integer/range validation remains authoritative.

Shared form styles align label, control and helper rows with subgrid, including wrapped labels and currency/date wrappers. Single-line paired controls have matching 44px heights. Mobile form action pairs share a row; long labels wrap inside equal-height buttons. Three-action dialogs retain three actions and header toolbars remain compact. Do not flatten currency or calendar wrappers or full-width imported-budget controls.

UI calendar dates display MM/DD/YYYY and month-only selections MM/YYYY. Date identifiers and API/storage values remain ISO; dates are not converted through the device timezone. DateInput/MonthInput show a formatted button with an explicit label, native picker on activation, and accessible fallback on ArrowDown or unsupported native picker. Optional fields can clear. Selection validates calendar dates and bounds before invoking the original change/navigation handler. Fallback calendars portal into native workout dialogs; Escape closes only the picker. Preserve dirty-date navigation and unconfirmed-save protections. Timestamp displays use an explicit timezone where the profile is available. Existing user notes and generated Markdown are not rewritten.

Verification uses synthetic local D1 and mocked AI. Check clear/retype/save and invalid-save retention, decimal loads, optional cardio distances, program save/reopen, both builder entry points, date selection/cancellation/filter clearing, wrapped labels at320px and390px, footer at changing viewport heights and workspace scroll boundaries, and no console errors. No migration, production personal-data write or paid AI call is needed.
