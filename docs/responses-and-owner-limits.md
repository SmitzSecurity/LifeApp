# Responses and owner prototype limits

September 15, 2026. The latest owner request replaces the inline editor and 80/20 split. Responses now shows yesterday's analysis above saved history, with Write a response (or Continue today's response if it exists). This opens a focused dialog; existing entries open in the same dialog with their analysis first. Empty entries omit the analysis placeholder. Saving closes the dialog and refreshes history. No automatic generation or new consent is introduced. Search/pagination and adjacent edit/trash icons remain; deletion requires confirmation and uses seven-day Trash.

The page scrolls naturally through analysis and history, retaining 60px rows and 44px edit/delete targets. The dialog starts at its content height and grows up to the visible viewport limit, following visualViewport resize/offset changes while the mobile keyboard is open. Its journal textarea begins at three rows (about100px) and expands for typed, pasted and provisional dictated text, including width changes. New lines at the end stay in view while typing or dictating; editing in the middle preserves position. The dialog's fields then scroll while Save, Cancel and voice controls stay visible. Opening and closing an editor preserves the list's scroll position and returns focus to its opener. X, Escape and Cancel discard unsaved changes; in-flight or unconfirmed saves keep the dialog and exact retry until reconciled. Changing dates retains the existing Save/Discard/Stay protection, with discard blocked for an unconfirmed write.

The microphone is at the dialog footer's bottom right: a22px visible circle within a44px touch target. It starts browser dictation only when clicked. A listening state and speech-activity bars distinguish an open microphone from detected speech; browser speech events and transcript arrivals drive the indicator, with reduced-motion support. This is activity feedback, not a measured audio-level meter. Words appear as processed, including provisional text; later results replace that phrase without duplication. Existing typed text remains before new speech. Tap again to stop, then review and Save. The journal and date cannot be edited during recording. Cancel, X and Escape synchronously abort recording before the dialog closing animation, detach speech callbacks and clear activity timers; unmount is also guarded. Permission denial and the6,000-character limit preserve text. Unsupported browsers retain a disabled microphone with keyboard-dictation guidance. Availability/permissions depend on the browser; no Gemini call or LifeApp AI credits are involved.

Repeatable browser verification: `node scripts/browser-smoke.mjs --calm --editing --analysis --history --dictation` serves a synthetic speech implementation and visible controls from the loopback-only fixture. No real microphone, production data or provider access is involved. Controls move into the active journal dialog's focus scope. Verify speech detected/paused, provisional text/corrections, final replacement, auto-growth/shrink, Save, Cancel/X/Escape/late results, switching entries, denial, limits and unsupported fallback. Close the synthetic control panel before clicking underlying form buttons; keyboard activation also avoids its overlay. Add `--unconfirmed-entry` to lose the first successful entry acknowledgement and verify the UI keeps the pending exact retry; local status must show one new day at version1 after retry.

Settings uses a native timezone dropdown populated from the browser's IANA timezone list, with UTC, common fallbacks and the exact saved zone retained. Device timezone remains the automatic default. Manual choices are versioned with the profile and Cancel restores the saved selection. No timezone, consent or schedule is changed by installing this release.

## Temporary owner allowance

The operator can configure two private Worker runtime bindings: `LIFEAPP_AI_OWNER_USER_ID` (an existing opaque `google:` account ID) and `LIFEAPP_AI_OWNER_LIMITS_UNTIL` (an ISO UTC timestamp ending in `.000Z`). Resolve the ID against the existing verified Google account; never identify the owner by a client-supplied email or role, embed their email/ID in source, or link/merge accounts. Missing, malformed, non-Google or expired configuration grants no override.

Until expiry, only that account has 25 total AI attempts per UTC day, 10 attempts per builder purpose per UTC day, five regenerations per analysis period per UTC day, and a $5 account monthly cap. Everyone else retains 5 total, 2 per builder, 2 regenerations and $1 per month. The app-wide $5 monthly cap and $0.20 reservations remain unchanged. Automatic and manual analyses resolve the same account limits. Deleted/archived usage still counts, unknown outcomes retain holds, duplicate requests do not run again, and the cost circuit breaker is unchanged. This grants no automatic-analysis or email consent and purchases no provider credits.

The initial private override expires October 14, 2026 at 23:59:59 UTC. At expiry it falls back to standard limits without clearing any usage; an account already above the normal allowance waits for the relevant reset. Removing either binding disables the override immediately. Keep these runtime values out of generated configuration and public Git history.

## Launch reminder

Before the owner opens registration or enables customer payments, review and remove or deliberately replace the prototype override. Confirm the customer allowance, shared spending cap, payment enforcement and scheduled-analysis headroom against real usage. Recheck Gemini pricing before January 1, 2027; the existing pricing-expiry gate remains. Do not charge users or buy services as part of this release.

## Verification

208 tests pass (196 source + 12 compiled Worker). New tests exercise exact owner identity, invalid configuration, expiry, daily/build/regeneration limits, archived accounting, other-account isolation, global spending and the circuit breaker. Existing tests retain unknown-outcome and exact-retry coverage. Browser checks use synthetic local D1 with external AI mocked: new inline Save, historical Save, cancellation, confirmation, preserving another day's unsaved draft, dirty navigation, timezone Save/Cancel/persistence, 390px mobile and desktop layout. Production checks are read-only; activating the private owner binding is the only runtime change. No migration is required and 0000–0010 must not be repeated.

September15 dialog verification: full217 tests (205 source +12 compiled Worker), TypeScript and final Cloudflare build/dry run passed. Synthetic browser checks covered saved analysis above history, Write/Continue and historic Save, search/pagination/delete confirmation, date Stay/Discard/Save,101px empty textarea and30-line growth with a visible footer at390x500. Voice detected/paused, provisional/final replacement, immediate X/Escape cancellation and late callbacks, denied permission,6,000-character limit and unsupported fallback passed. Lost acknowledgement retry left one new day at version1; unconfirmed Cancel/Escape/date navigation stayed blocked. No production journal write, paid AI or real microphone recording was used for tests.
