# Budget provider repair and owner allowance — September 16, 2026

The owner approved one Budget AI recovery and approximately $1/day for development. The recovery was consumed but also returned an unknown outcome. Diagnosis found an invalid REST wire value: `generationConfig.responseFormat.text.mimeType` requires the enum `APPLICATION_JSON`. The prior `application/json` is valid for attachments and the legacy `responseMimeType` field, but not this enum. Both Budget and written-workout provider formatting now use the correct enum; ordinary analysis requests are unchanged.

Verified against Google's [live v1beta discovery](https://generativelanguage.googleapis.com/$discovery/rest?version=v1beta) and [REST reference](https://ai.google.dev/api/generate-content#TextResponseFormat). A public synthetic request with an intentionally invalid key reproduced HTTP400 invalid-enum before authentication. The corrected value advanced to the expected invalid-key rejection. This check could not generate content or incur usage. The previous mock incorrectly accepted the invalid value; tests now validate a pinned discovery enum and the compiled request body.

An explicit HTTP400 `INVALID_ARGUMENT` response without usage/candidates is a known rejection, recorded as `failed` / `provider_request_rejected` with zero usage and an exact replay that cannot dispatch again. Network/server errors, malformed errors and missing receipts retain unknown holds. Provider errors are not echoed with potentially private text. Historical attempts have no original receipt, so their statuses and null costs remain unchanged: an operator acknowledgment may release only the diagnosed original Budget request and its linked consumed recovery from admission blocking. Their two reservations remain in `life_ai_usage`; no request is resent automatically, no grant is renewed, and unrelated unknown attempts still block. The acknowledgment is not available through public writes and must round-trip through export.

The existing private owner override still expires at its configured date. While active, the owner has a $1 UTC-day cost ceiling (actual charges plus pending/unknown reservations), and calendar-month headroom of $1 times the number of days in that UTC month. Owner attempt limits remain 25/day, 10 per builder/day and five analysis regenerations/day. Ordinary accounts retain $1/account/month and the original $5/month shared ordinary pool. A bounded aggregate check includes both pools, and all accounting uses the ledger including archived attempts. Expiry restores baseline limits and counts all users in the original shared pool. No provider credit purchase, customer billing, consent change, migration or embedded owner identity is introduced. Review/remove the prototype override before opening paid registration.

Verification: 258 source tests and 17 compiled Worker tests pass (275 total), plus TypeScript, Cloudflare build and deployment dry run. Compiled tests reject the prior invalid MIME value, exercise large text/PDF and explicit zero-cost provider rejection, and preserve exact retries/export. Atomic limit tests cover concurrent admissions, automatic analysis, archived/unknown usage, ordinary-account isolation and expiry. D1 uses a shallower expression-depth limit than local SQLite; grouping the unchanged acknowledgment predicates keeps both real Worker admission and source tests within that limit. No production provider outcome was reclassified or reservation removed.
