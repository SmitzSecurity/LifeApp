# Budget activity workflow — September 15, 2026

Transaction names open their editor. Save, Void/Unvoid, Delete and Cancel share one retry-safe operation; Cancel is last. Void/Delete change the saved record without committing unrelated field drafts. Deleted records stay in the central seven-day Trash; deletion no longer adds a top-of-page success notice.

Expected payments have clickable names for schedule editing and right-aligned clickable amounts for confirmation. Variable estimates are prefilled and can be accepted unchanged. Current-month confirmations default to today; historical months retain a valid historical date. A future budget month explains that actuals belong to their payment month instead of presenting an impossible date range. Confirmed rows move below pending rows and use strikethrough. Pending rows sort by due date, with overdue items first; the next seven days use the theme warning color, and on/after the due day uses the destructive color. Status text also identifies urgency. Paused and off-month annual items remain editable under Other recurring items in the Expected section.

Future one-off entries saved through Quick transaction have optional `planned: true` and `expectedDate`. They remain pending until explicit confirmation even after their due day. They reserve category forecasts but never inflate recorded totals, recent activity, debt payments or actual AI evidence. Confirmation writes `planned: false`, preserving the expected date and the original month. Voided planned items remain accessible in history for Unvoid/Delete; deleted ones remain in Trash. Old transactions with neither field remain actuals. No migration is needed. Once planned items exist, do not roll back to code that ignores `planned` or rejects these fields.

Category bars show remaining allowance, clamped at zero. Expanded categories indent one list containing pending payments and actual transactions without duplicate section headers or duplicate confirmed payments. Separate scoped editors remain mounted when another view changes the saved record's category, kind or visibility. + Category is inside Edit categories; new categories, edits, deletions and optional ordering save atomically through category-edit. New draft removal discards only that unsaved row. Concurrent unrelated edits merge, and explicit ordering retains order conflicts and exact retries.

The month selector joins the totals. Add recurring item sits alongside expected income/expense totals. The duplicate recurring panel, Your month heading and estimates footer are removed by explicit owner request. Other proposed R1–R5 removals remain pending. Semantic theme colors, 44px controls, equal tile heights, pinned dialog actions and 64px navigation are preserved.

Validate synthetic mobile/desktop payment dialogs, estimate acceptance, early confirmation, due colors/order, future one-offs and void recovery, category add/cancel/save/order and remaining bars. Preserve annual occurrence IDs, existing saved months, independent drafts and unknown-outcome exact retries. Production checks are read-only; no migrations, consent changes, paid AI calls or live budget writes.

Verified 233 tests (221 source and 12 compiled Worker), TypeScript, standalone Cloudflare build and deployment dry run. Synthetic light/OLED checks at 320px, 390px and 1280px confirmed unchanged-estimate acceptance, current-day actual dates, one-off planning/early confirmation, Void/Unvoid recovery, no deletion notice, saved-base-only actions, overdue/7-day/8-day colors and ordering, full-to-empty remaining bars, atomic category add/reorder and draft removal, annual off-month access, and cross-view category draft preservation with stale-save rejection. At 320×430, fields scrolled while 44px actions stayed pinned; four actions fit one row at 320px. Desktop card pairs matched heights and all five metrics shared a row. Navigation remained 64px and flush, with no horizontal overflow or console errors. Physical Firefox Android toolbar behavior was not reproduced by desktop viewport checks; its existing geometry is unchanged.
