# LifeApp project continuity

Loan presets and assisted import: read docs/loan-builder.md. Optional debt loanType/paymentStatus/accruedInterestCents/interestAccrual retain legacy defaults; original principal and rate may be unknown. Balance-only loans create no expected payment and block first confirmation until a schedule is supplied; preserve historical corrections and paid identity guards. Credit-card debts are statement-only transfers, never expense duplication or guessed payoff. Keep principal/unpaid interest separate, no fabricated original balance/progress or repayment terms. Loan AI uses optional intent loans in the existing budget namespace, exact retry identity, shared caps/unknown holds and bounded file path; no separate allowance or provider schema. Require review/category assignment before atomic adoption. No migration; do not roll back to schemas rejecting these fields after saved use.

Recurring budget review and annual funds: read docs/budget-recurring-workflow.md. Weekly/biweekly/custom schedules use date-specific occurrence IDs while monthly/annual keep their original month IDs; preserve separate confirmations, income allocations, historical snapshots, exact retries and purge suppression. Credit-card payments are transfers, not additional spending or savings. AI expense categories require user assignment, with explicit inline creation and allowance adoption. A separately enabled account-wide annual-bills fund tracks confirmed tagged contributions/payments and derives its monthly target from included annual expenses; it never moves money. Preserve declined choices and historical fund records. No migration or incompatible rollback.

Owner development allowance and Budget AI repair: read docs/budget-provider-repair.md. The approved temporary owner allowance is $1 per UTC day, including reservations, with calendar-month headroom; ordinary accounts retain their original account/shared pool limits. Preserve the existing private owner identity and expiry. Budget AI uses the routine-builder JSON-instruction flow with its full schema and strict local validation; omit provider schema configuration for Budget. Only explicit HTTP400 INVALID_ARGUMENT receipts without generation data settle at zero cost. Historical diagnosed requests retain uncertain status and their full holds; narrowly validated operator acknowledgment may release only the named admission blockers. Never infer historical zero cost or automatically replay a request.

Budget files and income: read docs/budget-files-and-income.md and docs/income-planning.md. Preserve bounded full text/Office extraction, inline PDF/image fingerprints, exact retries, token preflight and shared caps. Operator-only budget recovery needs explicit authorization, never frees the old unknown reservation, and admits one fresh build; unrelated/uncertain recovery jobs still block. Optional incomePlan/incomeDetails calculate take-home from user-entered deductions. Savings/investment targets are planned and confirmed separately. Keep historical policy snapshots, stable allocation IDs, purge suppression and exports. Whole Month tile opens the guarded picker. No migrations or incompatible older rollback; R1–R5 remain pending.

Budget activity workflow: read docs/budget-activity-workflow.md. Future one-offs use optional planned/expectedDate, excluded from actual totals/activity/AI/debt until explicit confirmation; preserve expected date and original month. Voided planned items remain accessible in history. Expected amounts open confirmation, estimates are prefilled, confirmed rows sort last/strikethrough, next7days warning/on-or-after-due destructive. Category bars show remaining; activity is indented. + Category belongs inside atomic Edit categories, including new-item ordering/retries. Transaction names edit; Save/Void/Delete/Cancel share exact retry locks, with Cancel last. No migration; no rollback to code ignoring planned flags. Other R1–R5 removals remain pending.

App-wide UI spacing: read docs/app-wide-spacing.md. Keep density styles scoped, semantic theme colors, 44px touch targets, readable input text, fixed action bars and existing nav geometry. The owner requires approval before removing UI elements: docs/ui-simplification-review.md labels proposed removals R1–R5; these remain present pending review. Do not hide content as a spacing shortcut.

Annual subscriptions/mobile spacing: read docs/annual-subscriptions-and-mobile-spacing.md. Annual recurrences require month1–12, keep the full charge, appear only in renewal months, and count annual installments by eligible occurrence. Preserve monthly defaults, occurrence IDs, open actual drafts, exact retries and existing saved monthly plans. Annual loan tracking is rejected; debt math remains monthly. AI budget drafts support annual subscriptions. No migration; do not roll back before annual schema support after saved use. Mobile editors have equal pinned Cancel/Save actions, compact growing journal and consolidated workout overview; keep the confirmed64px navigation fix and semantic theme colors.

Mobile forms and dates: read docs/mobile-forms-and-dates.md. Browser navigation is64px with no added safe-area bottom padding; installed/fullscreen mode retains native insets. Workouts + New contains Manual/AI Build; Budget AI is beside Budget goals. NumericInput retains empty/decimal drafts; nonfinite numbers must be rejected before JSON serialization and exact retries preserved. Paired controls/actions align at mobile widths. UI dates use MM/DD/YYYY (month-only MM/YYYY), retaining ISO storage and dirty-date navigation. Date fallback portals stay inside native workout dialogs.

Category editor and previews: read docs/category-editor-and-previews.md and docs/appearance.md. The category-edit action atomically applies only this dialog's category changes and optional ID order; preserve exact retries, independent conflicts and archived references. Normal category expansion shows recorded/expected activity using the same occurrence IDs as the expected-payments section. Separate editor scopes preserve drafts across collapse/removal. Appearance previews open element-specific pickers; three saved theme slots share versioned Settings saves. Contrast suggestions are optional with Undo, replacing the former user checklist. Use synthetic preview data and safe base colors for editing controls. Journal dictation shows provisional text without repeated phrases; the latest response-page layout is documented below.

Responses and prototype limits: read docs/responses-and-owner-limits.md. Responses now shows yesterday's analysis above saved history and opens a dedicated response page from the bottom-right + or a saved entry, replacing the prior dialog. The journal grows with text; a small bottom-right microphone inside its border glows clearly when speech is detected without visible status text. The shared signed-in shell is viewport-pinned across all tabs: header/navigation are rows and the workspace scrolls. The editor has its own scrolling fields. Preserve workspace scroll/focus restoration and native pinch zoom. Back/Cancel discard edits and stop voice; unconfirmed saves retain exact retries. Saved entries use adjacent edit/trash icons and confirmed seven-day deletion. Preserve unrelated drafts. Settings timezone choices retain the saved IANA identifier. A private, expiring owner override uses verified opaque Google identity, never source-embedded email or browser claims. Defaults for other accounts, shared spending, archived usage, unknown holds and separate consents remain. When the owner confirms readiness for new users/payments, remind them to review/remove the prototype override and review customer limits before launch.

Muscle coverage: read docs/muscle-coverage.md. Start with an overview of all groups; a selection narrows details, and clicking away or the same map region clears it. Keep detail editing controls usable. Optional JSON muscle assignments, weeklySessions and warmup fields require no migration. Preserve existing workout snapshots and unknown-outcome retries. Direct + half indirect is an explicitly approximate estimate; the half-set rule comes from separate research, not ACSM. Unset routine frequency is not consent or a weekly schedule. Never infer lifting sets from written logs/cardio. Training-only analysis is now available on explicit request; finance-only analysis remains future work. Closing a dialog with X, Escape or Cancel discards unsaved edits and releases editor locks. Retain exact retries for in-flight or unconfirmed writes. Stop dictation and discard late callbacks on close.

Canonical repository: https://github.com/SmitzSecurity/LifeApp. The owner authorized replacing the abandoned Apps Script repository contents with the current LifeApp on September 8, 2026. Preserve the existing repository identity; do not create another Site or repository.

This is the standalone React/Vinext/Cloudflare Worker/D1 product. Use Node 24, npm ci, npm test, and npx tsc --noEmit. npm run dev and npm run build use the standalone configuration and require no Sites helpers. Runtime output is dist-standalone. The root Sites binding was removed intentionally during migration; do not recreate it. The old private Sites app remains a separate live fallback until account/data cutover.

Read README.md, docs/migration-to-codex.md, docs/data-migration-preview.md, and the latest available LifeApp_Product_Build_Log.md (stable ID libfile_19df04a785a0819185c2b0a728b24c08). The log records the verified remote commit and owner setup state. Git-backed code belongs here, not in separate Library archives.

Google login requires owner OAuth credentials and its exact registered callback. Fail closed if Google mode is absent; never trust oai-* headers, merge by email, allow arbitrary scopes, or put credentials in code/output. Server Gemini credentials and paid-project flags still require owner setup. Preserve deterministic adopted-habit scoring, incomplete-day gates, reports/revisions, usage reservations/caps and duplicate prevention.

Use synthetic fixtures. Migration preview does not authorize import or prove ownership; authenticated old/new account proof, atomic import and count/history reconciliation are still required. Never erase the old app/data before cutover. Do not buy services, charge customers or submit a public store release without specific authorization. Retired Sites tests in legacy/sites are historical checks, not the standalone test gate.

Daily scheduling: read docs/daily-scheduling.md and the latest build log. The owner completed migration 0005, configured one */5 * * * * Cron, activated planner/automatic flags and opted in on September 9, 2026. Automatic weekly analysis and email delivery were verified on September14,2026 under periods-v1; automatic daily generation still lacks an observed completed request. Preserve Cron and runtime flags. Do not equate a ready job or default preference with consent to transmit journals. Full-report emails require the separate full-report-v1 opt-in, migration 0007, an activated sender and the email runtime flag; the legacy preference never grants consent. Preserve held usage on unknown AI outcomes and do not resend unconfirmed emails. Migration 0004 includes a hand-authored trigger and eligibility view; preserve them.

Account deletion: read docs/account-deletion.md and docs/setup/account-deletion-0006.md. Migration 0006 must be verified before deploying this code. It does not delete accounts on installation. Actual deletion requires a recent verified Google session and exact confirmation, retires the opaque account ID, removes personal/auth records atomically, and preserves minimal AI accounting. Never test deletion on the owner's account. AI cost/rate limits and the circuit breaker must read life_ai_usage, including archived attempts. Older-code rollback must disable AI until it also includes archived usage.

Cloudflare Git build setup: docs/cloudflare-git-build.md. Use build:cloudflare and deploy:cloudflare with the existing D1 ID in a build variable. Generated config is ignored and keeps dashboard runtime vars; auth remains HTTP 503 until configured. No migrations run on initial deploy. Once the owner connects Git builds, main pushes may deploy: use review branches for further unfinished work rather than assuming main writes only publish source.

Analysis/home/budget release: read docs/analysis-and-budget.md. Migration 0008 was applied and verified on September 9, 2026; 0000–0008 must never be repeated on production. The owner explicitly changed the saved daily schedule to 04:00 America/New_York. Preserve the separate periods-v1 opt-in (initially off), full-report-v1 email opt-in, original analyses, cadence-aware revision keys and daily-only eligibility join. The product now calls reviews analysis and My setup Settings; Delete account is a discreet Settings action with the original safeguards. Core Journal/Movement/Money are always included, without automatic habit adoption. Variable recurring budget estimates are forecasts until actual confirmation. Archive category IDs; retain deleted transaction/recurring records for Restore and references. Budget, dashboard and analysis totals must exclude deleted transactions. Do not roll back to code that ignores deleted flags after the feature is used. These payload flags require no additional migration. Budget goals use the same versioned profile from both the Budget popup and Settings.

Budget item editing: categories and recurring items save independently through budget-item, comparing only the edited item and merging with the latest versioned month. Keep exact retries and same-item conflicts; never replace unrelated changes or commit another editor's draft. First-transaction Save initializes a missing month automatically. Preserve local transaction drafts when another action archives their category or removes their recurrence. There are no page-wide Save plan buttons. Currency inputs show a dollar prefix without redundant USD labels.

Budget layout: section collapse states are browser preferences; keep hidden editors mounted so collapsing does not discard drafts. Category ordering is saved per month through category-order, comparing only the previous ID order and retaining all category data, archived identities and references. Keep ordering conflicts separate from unrelated allowance edits.

Workout notes/routines: read docs/workout-notes-and-routines.md. Migration0009 was applied and verified September14,2026; never replay0000–0009. Shared Markdown rendering must keep raw HTML/images disabled. Written workout notes are versioned workout-note resources and bounded analysis evidence, never fabricated structured sets. Preserve old exercise snapshots (repMax is optional). Routine AI builds use a separate table, explicit per-request action, strict output validation, namespaced idempotent IDs and the shared life_ai_usage ledger. Preserve all routine usage through account deletion and on code rollback; unknown outcomes retain reservations and block new builds. Drafts require separate review/save before becoming routines.

Workout logging / deletion: read docs/workout-logging.md. Warm-ups do not consume planned working sets; setNumber is a stable serial (1–40), not a working-set ordinal. Confirmed workout-note.structured is projected once into actual volume/activity; never infer sets from unstructured notes. Personal preset definitions remain with source logs, including Trash. workout: and training: jobs share life_routine_builds and life_ai_usage; filter namespaces when listing, preserve unknown reservations and all limits through deletion. Training analysis runs only on explicit request. Entry deletion preserves snapshots, sets complete=false and requires versioned Restore; resource deletion clears active_slot and excludes totals. Analysis/build visibility markers retain content for Restore, accounting and original revision keys. Deleted analyses cannot be prior evidence or claimed for pending email; cancelled email is never resent on Restore. These additions require no migration. Do not roll back to code that ignores new flags/namespaces or assumes setNumber<=planned sets after feature use.

Central Trash: read docs/central-trash.md. The owner now requests seven-day Trash retention and removal of prior generated text on successful regeneration, superseding earlier original-content retention instructions. Migration0010 was applied and verified September14,2026. Never replay migrations0000–0010. Keep the existing triggers and table intact. Permanent deletion must preserve usage/unknown holds/revision IDs while scrubbing AI content, and must block stale resurrection. Trash is a single top-left menu. Organize with AI for written workouts is removed at both UI and HTTP boundary; preserve existing structured notes and routine/training AI.

Appearance: read docs/appearance.md. OLED is the default. Optional versioned profile appearance stores separate per-mode color overrides; validate known tokens and six-digit hex only. Theme preview is local until Save Settings; Cancel restores the saved account palette. Keep active CSS and the muscle map on semantic variables, including portal dialogs. No migration.
