# LifeApp project continuity

Canonical repository: https://github.com/SmitzSecurity/LifeApp. The owner authorized replacing the abandoned Apps Script repository contents with the current LifeApp on September 8, 2026. Preserve the existing repository identity; do not create another Site or repository.

This is the standalone React/Vinext/Cloudflare Worker/D1 product. Use Node 24, npm ci, npm test, and npx tsc --noEmit. npm run dev and npm run build use the standalone configuration and require no Sites helpers. Runtime output is dist-standalone. The root Sites binding was removed intentionally during migration; do not recreate it. The old private Sites app remains a separate live fallback until account/data cutover.

Read README.md, docs/migration-to-codex.md, docs/data-migration-preview.md, and the latest available LifeApp_Product_Build_Log.md (stable ID libfile_19df04a785a0819185c2b0a728b24c08). The log records the verified remote commit and owner setup state. Git-backed code belongs here, not in separate Library archives.

Google login requires owner OAuth credentials and its exact registered callback. Fail closed if Google mode is absent; never trust oai-* headers, merge by email, allow arbitrary scopes, or put credentials in code/output. Server Gemini credentials and paid-project flags still require owner setup. Preserve deterministic adopted-habit scoring, incomplete-day gates, reports/revisions, usage reservations/caps and duplicate prevention.

Use synthetic fixtures. Migration preview does not authorize import or prove ownership; authenticated old/new account proof, atomic import and count/history reconciliation are still required. Never erase the old app/data before cutover. Do not buy services, charge customers or submit a public store release without specific authorization. Retired Sites tests in legacy/sites are historical checks, not the standalone test gate.

Daily scheduling: read docs/daily-scheduling.md and the latest build log. The owner completed migration 0005, configured one */5 * * * * Cron, activated planner/automatic flags and opted in on September 9, 2026. Actual scheduled report generation is still awaiting observation. Preserve Cron and runtime flags. Do not equate a ready job or default preference with consent to transmit journals. Full-report emails require the separate full-report-v1 opt-in, migration 0007, an activated sender and the email runtime flag; the legacy preference never grants consent. Preserve held usage on unknown AI outcomes and do not resend unconfirmed emails. Migration 0004 includes a hand-authored trigger and eligibility view; preserve them.

Account deletion: read docs/account-deletion.md and docs/setup/account-deletion-0006.md. Migration 0006 must be verified before deploying this code. It does not delete accounts on installation. Actual deletion requires a recent verified Google session and exact confirmation, retires the opaque account ID, removes personal/auth records atomically, and preserves minimal AI accounting. Never test deletion on the owner's account. AI cost/rate limits and the circuit breaker must read life_ai_usage, including archived attempts. Older-code rollback must disable AI until it also includes archived usage.

Cloudflare Git build setup: docs/cloudflare-git-build.md. Use build:cloudflare and deploy:cloudflare with the existing D1 ID in a build variable. Generated config is ignored and keeps dashboard runtime vars; auth remains HTTP 503 until configured. No migrations run on initial deploy. Once the owner connects Git builds, main pushes may deploy: use review branches for further unfinished work rather than assuming main writes only publish source.

Analysis/home/budget release: read docs/analysis-and-budget.md. Migration 0008 was applied and verified on September 9, 2026; 0000–0008 must never be repeated on production. The owner explicitly changed the saved daily schedule to 04:00 America/New_York. Preserve the separate periods-v1 opt-in (initially off), full-report-v1 email opt-in, original analyses, cadence-aware revision keys and daily-only eligibility join. The product now calls reviews analysis and My setup Settings; Delete account is a discreet Settings action with the original safeguards. Core Journal/Movement/Money are always included, without automatic habit adoption. Variable recurring budget estimates are forecasts until actual confirmation. Archive category IDs; retain deleted transaction/recurring records for Restore and references. Budget, dashboard and analysis totals must exclude deleted transactions. Do not roll back to code that ignores deleted flags after the feature is used. These payload flags require no additional migration. Budget goals use the same versioned profile from both the Budget popup and Settings.

Budget item editing: categories and recurring items save independently through budget-item, comparing only the edited item and merging with the latest versioned month. Keep exact retries and same-item conflicts; never replace unrelated changes or commit another editor's draft. First-transaction Save initializes a missing month automatically. Preserve local transaction drafts when another action archives their category or removes their recurrence. There are no page-wide Save plan buttons. Currency inputs show a dollar prefix without redundant USD labels.
